// In javascript null is not a reference to a non-existing object or a null pointer in other languages.
// Null is a special value which just represents nothing, empty.

let age  = null
console.log(typeof(age))

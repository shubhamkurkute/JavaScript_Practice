// Null Coalescence is a type of condition which is based on null value.
//i.e. if variable contains no value i.e. null value than it returns another value 

let userName = "Shubham"

console.log(userName??"Anonymus") // As userName is not null it returns userName otherwise it would have return Anonymous.